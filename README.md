# Windows Standard I/O Handle Debugging Helper

This is a tiny executable that prints information about the
type of Handle used for stdout to stderr on Windows.

## Installation

```console
cargo install win-stdio-handle-dbg
```
